1.目前这个板子，因为我把V307的tx1、rx1和v305的串口连接在一起了，v305进入调试器模式时，
没有消息时，会持续控制rx1这根线的电平，导致rx1外接tx无效。这是硬件上没考虑到的问题，
引脚电平冲突。目前不换板子的解决办法就是要直接使用这个串口对外通信，那么让v305进入IAP
模式，rx1不会再工作在串口模式下，此时v305的tx引脚就不会拉住电平了，外部的tx就了可以
控制rx1进入接收中断了。
